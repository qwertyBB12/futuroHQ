---
phase: 10-video-pipeline-execution
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: false
requirements: [VPIPE-01, VPIPE-02]

must_haves:
  truths:
    - "All 26 video documents have fullText populated from enriched JSON transcripts"
    - "All 26 video documents have speakerSegments array populated with speaker/start/end/text items"
    - "All 26 video documents have b2Key and cdnUrl already populated (from populate-sanity-videos.py)"
    - "Videos missing b2Key or transcript appear in Needs Enrichment desk list"
  artifacts: []
  key_links:
    - from: "scripts/ingest-transcripts.ts"
      to: "Sanity video documents"
      via: "patches fullText + speakerSegments"
      pattern: "client\\.patch"
---

<objective>
Run the transcript ingestion script in live mode and verify all 26 video documents have B2 fields + transcripts populated.

Purpose: Execute the pipeline to populate transcript data into the 26 existing video drafts, then verify the full data state — b2Key, cdnUrl, fullText, speakerSegments all present on every B2-sourced video.

Output: All 26 video docs patched with transcript data; verification queries confirm completeness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-video-pipeline-execution/10-01-SUMMARY.md

@scripts/ingest-transcripts.ts
@lib/completeness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run transcript ingestion in live mode</name>
  <files>scripts/ingest-transcripts.ts</files>
  <read_first>
    scripts/ingest-transcripts.ts
  </read_first>
  <action>
Run the transcript ingestion script without --dry-run to patch all 26 video documents:

```bash
npx tsx scripts/ingest-transcripts.ts
```

This will:
- Fetch all B2-sourced video docs from Sanity
- Match each to its .enriched.json file by b2Key stem
- Patch fullText and speakerSegments onto each video doc
- Report results (matched, patched, skipped, unmatched)

Expected: 26 videos matched, 26 patched (or 25 if hb_4k had issues with case sensitivity — if so, note and investigate).

If any failures, check the error output and retry failed items with --force flag.
  </action>
  <verify>
    <automated>npx tsx scripts/ingest-transcripts.ts --dry-run 2>&1 | grep -E "already|matched|patched|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - Script completes without uncaught errors
    - Output shows 26 videos matched and patched (or explains any gaps)
    - Re-running with --dry-run shows all 26 already have fullText (skip message)
  </acceptance_criteria>
  <done>All 26 video documents patched with fullText and speakerSegments from enriched JSON files</done>
</task>

<task type="auto">
  <name>Task 2: Verify B2 fields and transcript data via GROQ</name>
  <files></files>
  <read_first>
    lib/completeness.ts
    scripts/populate-sanity-videos.py
  </read_first>
  <action>
Run GROQ verification queries against Sanity to confirm all 26 B2 videos have the required fields. Use the Sanity CLI or curl:

**Query 1: Count B2 videos with all required fields**
```groq
count(*[_type == "video" && videoSource == "b2" && defined(b2Key) && b2Key != "" && defined(cdnUrl) && cdnUrl != "" && defined(fullText) && fullText != ""])
```
Expected result: 26

**Query 2: Find any B2 videos MISSING b2Key or cdnUrl**
```groq
*[_type == "video" && videoSource == "b2" && (!defined(b2Key) || b2Key == "" || !defined(cdnUrl) || cdnUrl == "")]{_id, title, b2Key, cdnUrl}
```
Expected result: empty array []

**Query 3: Find any B2 videos MISSING transcript**
```groq
*[_type == "video" && videoSource == "b2" && (!defined(fullText) || fullText == "")]{_id, title}
```
Expected result: empty array []

**Query 4: Spot-check speakerSegments on one doc**
```groq
*[_type == "video" && videoSource == "b2" && defined(speakerSegments)][0]{title, "segmentCount": count(speakerSegments), "firstSegment": speakerSegments[0]}
```
Expected: segmentCount > 0, firstSegment has speaker, start, end, text fields.

Run via: `npx sanity documents query '<groq>'` or curl against the API.

If Query 2 or 3 returns any documents, those are gaps that need investigation — the populate-sanity-videos.py should have set b2Key/cdnUrl on all 26, and the ingestion script should have set fullText on all 26.

Note: VPIPE-01 covers b2Key, cdnUrl, and bunnyStatus — all set by populate-sanity-videos.py. thumbnailUrl is a separate concern covered by VMETA-03 in Phase 11.
  </action>
  <verify>
    <automated>npx sanity documents query '*[_type == "video" && videoSource == "b2" && (!defined(b2Key) || !defined(cdnUrl) || !defined(fullText))]{_id, title}' 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Query 1 returns 26 (all B2 videos have b2Key + cdnUrl + fullText)
    - Query 2 returns empty array (no B2 video missing b2Key or cdnUrl)
    - Query 3 returns empty array (no B2 video missing fullText)
    - Query 4 shows speakerSegments with speaker/start/end/text fields
  </acceptance_criteria>
  <done>All 26 B2-sourced video documents verified to have b2Key, cdnUrl, fullText, and speakerSegments populated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies transcript data in Studio UI</name>
  <files></files>
  <action>
Present verification results to user and ask them to confirm in Studio UI.
  </action>
  <verify>User confirms transcript data visible in Studio</verify>
  <done>User approves transcript data and B2 field population in Studio</done>
  <what-built>Transcript data patched into all 26 video documents. B2 fields (b2Key, cdnUrl) confirmed present. Completeness config updated to track b2Key.</what-built>
  <how-to-verify>
    1. Open Sanity Studio at https://hq.benextglobal.com
    2. Navigate to Media and Content > Videos
    3. Open any video (e.g., "Alistair Coll - Futuro MMXIX Testimonial")
    4. Click the "Transcript" tab — verify fullText is populated with transcript text
    5. Verify speakerSegments shows collapsible speaker entries with timestamps
    6. Check the "Needs Enrichment" section in the desk — videos missing tags/description/thumbnail should appear, but NOT for missing b2Key/cdnUrl/transcript (those are now filled)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
1. All 26 video docs have fullText (GROQ count query = 26)
2. All 26 video docs have b2Key and cdnUrl (GROQ gap query returns [])
3. speakerSegments have correct shape (_key, _type, speaker, start, end, text)
4. Updated completeness config correctly surfaces videos missing b2Key in Needs Enrichment
</verification>

<success_criteria>
- Transcript ingestion script ran successfully, patching 26 video documents
- GROQ verification confirms zero gaps in b2Key, cdnUrl, and fullText across all B2 videos
- User confirms transcript data visible in Studio UI
</success_criteria>

<output>
After completion, create `.planning/phases/10-video-pipeline-execution/10-02-SUMMARY.md`
</output>
