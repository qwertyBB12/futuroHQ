---
phase: 09-transcript-podcast-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemaTypes/blocks/transcriptBlock.ts
  - schemaTypes/video.ts
  - schemaTypes/podcastEpisode.ts
  - components/inputs/TranscriptSegmentsInput.tsx
autonomous: true
requirements: [TRANS-01, TRANS-03, POD-01, POD-02]

must_haves:
  truths:
    - "Video document shows fullText and speakerSegments fields in Studio"
    - "Speaker segments render as a collapsible, read-only list with speaker/time/text"
    - "PodcastEpisode document shows the same transcript fields as video"
    - "PodcastEpisode document shows externalLinks array with platform + URL pairs"
  artifacts:
    - path: "schemaTypes/blocks/transcriptBlock.ts"
      provides: "Shared transcript field definitions (fullText + speakerSegments)"
      exports: ["transcriptFields"]
    - path: "components/inputs/TranscriptSegmentsInput.tsx"
      provides: "Custom read-only collapsible renderer for speaker segments"
      exports: ["TranscriptSegmentsInput"]
    - path: "schemaTypes/video.ts"
      provides: "Video schema with transcript fields added"
    - path: "schemaTypes/podcastEpisode.ts"
      provides: "PodcastEpisode schema with transcript + externalLinks fields"
  key_links:
    - from: "schemaTypes/blocks/transcriptBlock.ts"
      to: "schemaTypes/video.ts"
      via: "spread into fields array"
      pattern: "\\.\\.\\.transcriptFields"
    - from: "schemaTypes/blocks/transcriptBlock.ts"
      to: "schemaTypes/podcastEpisode.ts"
      via: "spread into fields array"
      pattern: "\\.\\.\\.transcriptFields"
    - from: "components/inputs/TranscriptSegmentsInput.tsx"
      to: "schemaTypes/blocks/transcriptBlock.ts"
      via: "components.input on speakerSegments field"
      pattern: "TranscriptSegmentsInput"
---

<objective>
Add transcript fields (fullText + speakerSegments) to video and podcastEpisode schemas via a shared block, with a custom read-only collapsible component for speaker segments. Add externalLinks array to podcastEpisode.

Purpose: Schema must be ready to accept pipeline transcript data (Phase 10) and allow editors to add podcast platform links.
Output: Shared transcriptBlock, custom TranscriptSegmentsInput component, updated video + podcastEpisode schemas.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@schemaTypes/blocks/commonMeta.ts
@schemaTypes/blocks/governanceBlock.ts
</context>

<interfaces>
<!-- Existing pattern: shared field blocks are arrays of defineField() spread into schema fields -->

From schemaTypes/blocks/governanceBlock.ts (pattern to follow):
```typescript
import { defineField } from 'sanity'
export const governanceFields = [
  defineField({ name: '...', ... }),
  // ...
]
```

Used in schemas as: `...governanceFields`

From enriched JSON (transcript data shape to match):
```json
{
  "full_text": "string — concatenated transcript text",
  "speaker_segments": [
    { "speaker": "SPEAKER_00", "start": 0.0, "end": 30.38, "text": "..." }
  ]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create transcriptBlock shared fields and TranscriptSegmentsInput component</name>
  <files>schemaTypes/blocks/transcriptBlock.ts, components/inputs/TranscriptSegmentsInput.tsx</files>
  <read_first>
    schemaTypes/blocks/governanceBlock.ts
    schemaTypes/blocks/commonMeta.ts
    components/inputs/MediaBlockInput.tsx
    transcripts/C3460.enriched.json
  </read_first>
  <action>
    Create `schemaTypes/blocks/transcriptBlock.ts` exporting `transcriptFields` as an array of defineField() calls (same pattern as governanceFields):

    1. `fullText` field:
       - name: 'fullText'
       - title: 'Full Transcript'
       - type: 'text'
       - description: 'Complete transcript text (populated by pipeline)'
       - readOnly: true
       - options: { collapsible: true, collapsed: true } (via wrapping in object — but since this is a top-level text field, use rows: 10)
       - group: 'transcript'

    2. `speakerSegments` field:
       - name: 'speakerSegments'
       - title: 'Speaker Segments'
       - type: 'array'
       - description: 'Speaker-diarized transcript segments (populated by pipeline)'
       - readOnly: true
       - of: [{ type: 'object', fields: [...] }] with inline object fields:
         - speaker: string, title 'Speaker'
         - start: number, title 'Start (seconds)'
         - end: number, title 'End (seconds)'
         - text: text, title 'Text'
       - The inline object preview should show: title = speaker, subtitle = `${start}s - ${end}s`
       - components: { input: TranscriptSegmentsInput } on the array field
       - group: 'transcript'

    3. Export `transcriptGroup` as `{ name: 'transcript', title: 'Transcript' }` so schemas can add the group.

    Create `components/inputs/TranscriptSegmentsInput.tsx`:
    - Import `ArrayOfObjectsInputProps` from 'sanity'
    - Import `Card, Stack, Flex, Text, Box` from '@sanity/ui'
    - Import `useState` from 'react'
    - Component receives ArrayOfObjectsInputProps
    - Renders a collapsible card-based list:
      - Header: "Speaker Segments ({count})" with a toggle button (Expand All / Collapse All)
      - Each segment as a Card with:
        - Flex row: speaker label (bold, Copper #B17E68 color), time range in muted text (Archival Slate #8B8985)
        - Text content below the header row
        - Segment cards have `padding={3}`, `radius={2}`, `tone="transparent"`
        - Border between segments using `borderBottom`
    - If no segments (empty array or undefined), show muted text: "No transcript segments — run pipeline to populate"
    - The component is READ-ONLY display — no editing controls, no drag handles
    - Do NOT call props.renderDefault(props) — this is a full custom renderer
    - Use inline styles for colors since these are Civic Modern tokens not in Sanity's theme
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && grep -c "defineField" schemaTypes/blocks/transcriptBlock.ts && grep -c "export" schemaTypes/blocks/transcriptBlock.ts && grep -c "TranscriptSegmentsInput" components/inputs/TranscriptSegmentsInput.tsx</automated>
  </verify>
  <acceptance_criteria>
    - schemaTypes/blocks/transcriptBlock.ts contains `export const transcriptFields`
    - schemaTypes/blocks/transcriptBlock.ts contains `export const transcriptGroup`
    - schemaTypes/blocks/transcriptBlock.ts contains `name: 'fullText'` with `type: 'text'` and `readOnly: true`
    - schemaTypes/blocks/transcriptBlock.ts contains `name: 'speakerSegments'` with `type: 'array'` and `readOnly: true`
    - schemaTypes/blocks/transcriptBlock.ts contains inline object fields: `name: 'speaker'`, `name: 'start'`, `name: 'end'`, `name: 'text'`
    - schemaTypes/blocks/transcriptBlock.ts imports TranscriptSegmentsInput from components/inputs
    - components/inputs/TranscriptSegmentsInput.tsx exports a function component named TranscriptSegmentsInput
    - components/inputs/TranscriptSegmentsInput.tsx contains '#B17E68' (Copper) and '#8B8985' (Archival Slate)
    - components/inputs/TranscriptSegmentsInput.tsx imports from '@sanity/ui'
  </acceptance_criteria>
  <done>Shared transcript block and custom component exist, ready to be consumed by schemas</done>
</task>

<task type="auto">
  <name>Task 2: Add transcript fields to video schema and transcript + externalLinks to podcastEpisode schema</name>
  <files>schemaTypes/video.ts, schemaTypes/podcastEpisode.ts</files>
  <read_first>
    schemaTypes/video.ts
    schemaTypes/podcastEpisode.ts
    schemaTypes/blocks/transcriptBlock.ts
  </read_first>
  <action>
    **video.ts changes (per TRANS-01):**
    1. Add import: `import { transcriptFields, transcriptGroup } from './blocks/transcriptBlock'`
    2. Add `transcriptGroup` to the `groups` array (after 'storage')
    3. Spread `...transcriptFields` into the fields array — place AFTER the `duration` field and BEFORE the `tags` field
    4. Do NOT change any existing fields

    **podcastEpisode.ts changes (per POD-01, POD-02):**
    1. Add import: `import { transcriptFields, transcriptGroup } from './blocks/transcriptBlock'`
    2. Add a `groups` array to the schema (it currently has none besides 'distribution'):
       - Keep existing: `{ name: 'distribution', title: 'Distribution' }`
       - Add: transcriptGroup
    3. Spread `...transcriptFields` into the fields array — place AFTER `videoEmbed` and BEFORE `series`
    4. Add `externalLinks` array field AFTER `audioEmbed` and BEFORE `videoEmbed`:
       ```
       defineField({
         name: 'externalLinks',
         title: 'External Platform Links',
         type: 'array',
         description: 'Links to this episode on external platforms (Spotify, Apple Podcasts, etc.)',
         group: 'distribution',
         of: [{
           type: 'object',
           fields: [
             defineField({
               name: 'platform',
               title: 'Platform',
               type: 'string',
               options: {
                 list: [
                   { title: 'Spotify', value: 'spotify' },
                   { title: 'Apple Podcasts', value: 'apple' },
                   { title: 'Google Podcasts', value: 'google' },
                   { title: 'YouTube', value: 'youtube' },
                   { title: 'Amazon Music', value: 'amazon' },
                   { title: 'Captivate', value: 'captivate' },
                   { title: 'Other', value: 'other' },
                 ],
               },
               validation: (Rule) => Rule.required(),
             }),
             defineField({
               name: 'url',
               title: 'URL',
               type: 'url',
               validation: (Rule) => Rule.required(),
             }),
           ],
           preview: {
             select: { title: 'platform', subtitle: 'url' },
           },
         }],
       })
       ```
    5. Do NOT remove or change any existing fields
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && grep "transcriptFields" schemaTypes/video.ts && grep "transcriptFields" schemaTypes/podcastEpisode.ts && grep "externalLinks" schemaTypes/podcastEpisode.ts && grep "transcriptGroup" schemaTypes/video.ts && grep "transcriptGroup" schemaTypes/podcastEpisode.ts</automated>
  </verify>
  <acceptance_criteria>
    - schemaTypes/video.ts contains `import { transcriptFields, transcriptGroup } from './blocks/transcriptBlock'`
    - schemaTypes/video.ts contains `...transcriptFields` in the fields array
    - schemaTypes/video.ts contains `transcriptGroup` in the groups array
    - schemaTypes/podcastEpisode.ts contains `import { transcriptFields, transcriptGroup } from './blocks/transcriptBlock'`
    - schemaTypes/podcastEpisode.ts contains `...transcriptFields` in the fields array
    - schemaTypes/podcastEpisode.ts contains `transcriptGroup` in the groups array
    - schemaTypes/podcastEpisode.ts contains `name: 'externalLinks'` with `type: 'array'`
    - schemaTypes/podcastEpisode.ts contains platform options: 'spotify', 'apple', 'google', 'youtube', 'amazon', 'captivate', 'other'
    - schemaTypes/podcastEpisode.ts still contains all original fields (title, slug, description, pubDate, episodeNumber, etc.)
    - schemaTypes/video.ts still contains all original fields (title, slug, videoSource, b2Key, cdnUrl, etc.)
  </acceptance_criteria>
  <done>Video and podcastEpisode schemas both have transcript fields and podcastEpisode has externalLinks — Studio renders the new field groups</done>
</task>

<task type="auto">
  <name>Task 3: Verify Studio builds with new schema fields</name>
  <files></files>
  <read_first>
    schemaTypes/blocks/transcriptBlock.ts
    schemaTypes/video.ts
    schemaTypes/podcastEpisode.ts
  </read_first>
  <action>
    Run the Sanity Studio TypeScript type check to verify all schema changes compile without errors:
    1. Run `npx tsc --noEmit` (or the project's configured type check command)
    2. If type errors exist, fix them in the relevant files
    3. Run `npx sanity build` with `--yes` flag if needed, to verify Studio can build
    4. If build fails, diagnose and fix — common issues: missing imports, incorrect field type names, component prop type mismatches
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && npx sanity build --yes 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx sanity build --yes` exits with code 0
    - Build output directory is created without errors
  </acceptance_criteria>
  <done>Studio builds successfully with transcript fields on video and podcastEpisode, externalLinks on podcastEpisode, and custom TranscriptSegmentsInput component</done>
</task>

</tasks>

<verification>
1. `npx sanity build --yes` succeeds — schema is valid
2. `grep -r "transcriptFields" schemaTypes/` shows video.ts and podcastEpisode.ts both import and spread it
3. `grep "externalLinks" schemaTypes/podcastEpisode.ts` confirms field exists
4. `grep "TranscriptSegmentsInput" schemaTypes/blocks/transcriptBlock.ts` confirms custom component is wired
</verification>

<success_criteria>
- Video schema has fullText (text, readOnly) and speakerSegments (array, readOnly with custom component) fields
- PodcastEpisode schema has identical transcript fields
- PodcastEpisode schema has externalLinks array with platform + URL object items
- Custom TranscriptSegmentsInput renders segments as collapsible read-only list with speaker/time/text
- Studio builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-transcript-podcast-schema/09-01-SUMMARY.md`
</output>
