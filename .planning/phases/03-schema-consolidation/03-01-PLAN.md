---
phase: 03-schema-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemaTypes/socialPost.ts
  - schemaTypes/curatedPost.ts
  - schemaTypes/vlog.ts
  - schemaTypes/person.ts
  - schemaTypes/alumni.ts
  - schemaTypes/ledgerPerson.ts
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02

must_haves:
  truths:
    - "socialPost, curatedPost, and vlog schemas have only reference-based tags (no string array tags field)"
    - "tags_ref field has been renamed to tags on socialPost, curatedPost, and vlog"
    - "person, alumni, and ledgerPerson each have a clear description documenting their distinct purpose"
    - "person and ledgerPerson remain ungoverned (no governanceFields) with documented rationale"
  artifacts:
    - path: "schemaTypes/socialPost.ts"
      provides: "Social post schema with consolidated tags"
      contains: "tags.*reference.*tag"
    - path: "schemaTypes/curatedPost.ts"
      provides: "Curated post schema with consolidated tags"
      contains: "tags.*reference.*tag"
    - path: "schemaTypes/vlog.ts"
      provides: "Legacy vlog schema with consolidated tags"
      contains: "tags.*reference.*tag"
    - path: "schemaTypes/person.ts"
      provides: "Person schema with rationale description"
      contains: "cross-entity identity"
    - path: "schemaTypes/alumni.ts"
      provides: "Alumni schema with rationale description"
      contains: "Companion Platform"
    - path: "schemaTypes/ledgerPerson.ts"
      provides: "Ledger person schema with rationale description"
      contains: "Vanguard Ledger"
  key_links: []
---

<objective>
Consolidate the dual tag system and document person type separation rationale.

Purpose: Eliminate schema ambiguity where socialPost, curatedPost, and vlog have both string `tags` and reference `tags_ref` fields. Also provide clear documentation for why person, alumni, and ledgerPerson are intentionally separate types.

Output: 6 updated schema files with clean tag fields and documented type descriptions.
</objective>

<execution_context>
@/Users/hectorhlopez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hectorhlopez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-schema-consolidation/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate tag fields on socialPost, curatedPost, and vlog</name>
  <files>schemaTypes/socialPost.ts, schemaTypes/curatedPost.ts, schemaTypes/vlog.ts</files>
  <action>
For each of the three schema files (socialPost.ts, curatedPost.ts, vlog.ts):

1. **Remove** the legacy string `tags` field — the one defined as `type: 'array', of: [{ type: 'string' }]`
2. **Rename** `tags_ref` to `tags` — change both `name: 'tags_ref'` to `name: 'tags'` and `title: 'Tags (ref)'` to `title: 'Tags'`
3. **Update the description** on the renamed tags field to remove migration language. New description: `'Reusable tag references for categorization and filtering'`
4. **Do NOT touch** the `legacyVlog` nested object inside video.ts — that is preserved as-is per user decision
5. **Do NOT touch** the `tagsOrHandles` field on socialPost — that is a separate field for hashtags/handles, not part of the dual-tag debt
6. **Do NOT touch** the `hashtags` field on socialPost — same reason

Existing string tag data stays in the content lake (Sanity preserves data when schema fields are removed). This is schema-only cleanup.
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && npx sanity schema extract --enforce-required-fields 2>&1 | head -20 && grep -c "tags_ref" schemaTypes/socialPost.ts schemaTypes/curatedPost.ts schemaTypes/vlog.ts; echo "Expected: 0 occurrences of tags_ref in each file"</automated>
  </verify>
  <done>socialPost, curatedPost, and vlog schemas each have a single `tags` field using references to tag documents. No `tags_ref` field name remains. No legacy string `tags` array field remains (outside of legacyVlog nested object).</done>
</task>

<task type="auto">
  <name>Task 2: Document person type separation rationale</name>
  <files>schemaTypes/person.ts, schemaTypes/alumni.ts, schemaTypes/ledgerPerson.ts</files>
  <action>
Update the `description` field in each defineType() call to document the distinct purpose and intentional separation:

**person.ts** — Add/update description:
`'Simple public profiles (name, role, bio, photo) — cross-entity identity records shared across the ecosystem. Intentionally ungoverned: person is a cross-cutting identity type, not multi-entity content, so governanceFields are not applied.'`

**alumni.ts** — Update description to include separation rationale. Preserve existing AI Assist instructions but prepend:
`'Futuro/BeNeXT program graduates with Companion Platform integration (30+ fields including dreams, conversations, accreditation). Distinct from person (simple identity) and ledgerPerson (narrative intelligence). '` then keep the rest of the existing description.

**ledgerPerson.ts** — Add/update description:
`'Vanguard Ledger narrative intelligence dossiers — soulmarks, doctrine alignment, strategic grids. Distinct from person (simple identity) and alumni (program graduate profiles). Intentionally ungoverned: ledgerPerson is a cross-cutting identity type used for narrative intelligence, not multi-entity content.'`

These descriptions serve as documentation for why three separate types exist rather than one unified person type.
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && grep -A2 "description" schemaTypes/person.ts | head -5 && grep -A2 "description" schemaTypes/ledgerPerson.ts | head -5 && npx sanity schema extract --enforce-required-fields 2>&1 | head -5</automated>
  </verify>
  <done>All three person-like types have clear description fields documenting their distinct purpose, why they are intentionally separate, and why person/ledgerPerson lack governanceFields.</done>
</task>

</tasks>

<verification>
- `npx sanity schema extract --enforce-required-fields` completes without errors
- No `tags_ref` field name appears in socialPost.ts, curatedPost.ts, or vlog.ts
- No string-type `tags` array (of: [{ type: 'string' }]) exists in socialPost.ts, curatedPost.ts, or vlog.ts (outside legacyVlog)
- person.ts, alumni.ts, ledgerPerson.ts all have description fields with separation rationale
</verification>

<success_criteria>
- Tag consolidation: socialPost, curatedPost, vlog use only reference-based `tags` field
- Person documentation: All three types have clear descriptions explaining their distinct roles and governance status
- Schema validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-consolidation/03-01-SUMMARY.md`
</output>
