---
phase: 16-pipeline-documentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MEDIA-PIPELINE.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "A reader unfamiliar with the codebase can identify what each script does, its inputs, its outputs, and where it runs"
    - "A reader can process a new raw video using only the documented command without reading source code"
    - "The document reflects CRF-18-only encoding (no bitrate cap references)"
    - "pipeline.py is documented as the primary entry point with all flags"
    - "Environment variable requirements are listed (HF_TOKEN, SANITY_TOKEN, B2 CLI auth)"
    - "Troubleshooting section covers the 3-5 most common failure modes"
  artifacts:
    - path: "docs/MEDIA-PIPELINE.md"
      provides: "Full pipeline architecture + usage guide"
      contains: "pipeline.py"
  key_links:
    - from: "docs/MEDIA-PIPELINE.md"
      to: "scripts/pipeline.py"
      via: "usage examples"
      pattern: "python3 scripts/pipeline.py"
    - from: "docs/MEDIA-PIPELINE.md"
      to: "scripts/process-raw-video.py"
      via: "script reference"
      pattern: "process-raw-video.py"
---

<objective>
Update docs/MEDIA-PIPELINE.md to reflect all Phase 14 and Phase 15 changes and add a usage guide, so a new raw video can be processed correctly without reading script source code.

Purpose: Complete DOCS-01 (architecture doc) and DOCS-02 (usage guide) to close v1.3 milestone.
Output: Single updated docs/MEDIA-PIPELINE.md with architecture, script reference, usage guide, and troubleshooting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pipeline-documentation/16-CONTEXT.md
@.planning/phases/15-pipeline-automation/15-01-SUMMARY.md
@.planning/phases/15-pipeline-automation/15-02-SUMMARY.md

@docs/MEDIA-PIPELINE.md
@scripts/pipeline.py
@scripts/process-raw-video.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite architecture overview, pipeline flow diagram, script reference table, and compression settings</name>
  <files>docs/MEDIA-PIPELINE.md</files>
  <read_first>
    - docs/MEDIA-PIPELINE.md (current 170-line doc — update target)
    - scripts/pipeline.py (lines 1-80 for docstring and constants, lines 290-341 for argparse flags, lines 60-78 for Sanity constants)
    - scripts/process-raw-video.py (lines 23-42 for CAMERA_LUTS and constants, lines 47-66 for argparse flags)
    - .planning/phases/16-pipeline-documentation/16-CONTEXT.md (decisions D-01 through D-08)
  </read_first>
  <action>
Rewrite docs/MEDIA-PIPELINE.md sections: Overview, Pipeline Flow, Processing Scripts, and Compression Settings. Keep the following sections intact (they are still accurate): B2 Bucket Structure, Camera Profiles and LUTs, Sanity Video Schema, Current State.

**Overview section** (per D-08 — written for future collaborators who know tech but not this setup):
- Add 2-3 sentences explaining what the ecosystem is: video content from educational summits (Futuro, Kah Foundry) processed into streamable clips with transcripts, managed in Sanity CMS
- Keep existing B2/Bunny/Sanity source-of-truth lines
- Add a note: "The pipeline is a set of Python scripts run on a local Mac workstation. No cloud compute is used for processing."

**Pipeline Flow diagram** (per D-03 — enhanced ASCII, not Mermaid):
Replace the existing simple flow with this expanded diagram showing pipeline.py as orchestrator:

```
Raw footage (B2 raw/)
       |
       v
  [pipeline.py]  <-- single entry point
       |
       |--- Step 1: Download + Encode (process-raw-video.py)
       |      LUT color grade --> H.264 CRF 18 slow --> faststart
       |      Whisper transcribe --> pyannote diarize
       |      Output: _processed.mp4 + .enriched.json
       |
       |--- Step 2: Upload processed video to B2 (edited/)
       |
       |--- Step 3: Extract speaker clips (extract-speaker-clips.py)
       |      Reads .enriched.json manifest
       |      Output: per-speaker .mp4 clips + manifest.json
       |
       |--- Step 4: Upload clips to B2 (clips/)
       |
       |--- Step 5: Create Sanity draft documents (--dry-run / --live)
       |      Video doc: b2Key, cdnUrl, fullText, speakerSegments
       |      Clip docs: b2Key, cdnUrl, duration, featuredIn=[]
       |
       v
  Sanity Studio --> Editorial review --> Publish
       |
       v
  Bunny CDN (benext.b-cdn.net) <-- pulls from B2 on first request
```

**Processing Scripts table** — Replace the existing table with an updated version. The table must have columns: Script, Purpose, Standalone Usage, Used by pipeline.py. Scripts to include:

| Script | Purpose | Standalone Usage | Used by pipeline.py |
|--------|---------|------------------|---------------------|
| `pipeline.py` | End-to-end orchestrator: encode + transcribe + clips + B2 upload + Sanity docs | `python3 scripts/pipeline.py "B2/path" --camera sony-a6700-slog3 --live` | N/A (this IS the entry point) |
| `process-raw-video.py` | Download from B2, apply LUT, encode H.264 CRF 18, transcribe (Whisper medium), diarize (pyannote) | `python3 scripts/process-raw-video.py "B2/path" --camera canon-r5-clog3` | Yes — called via `process_video()` |
| `extract-speaker-clips.py` | Cut per-speaker clips from diarized video using .enriched.json timestamps | `python3 scripts/extract-speaker-clips.py --all` | Yes — called via `process_transcript()` |
| `extract-dialogue-clips.py` | Cut multi-speaker dialogue segments | `python3 scripts/extract-dialogue-clips.py` | No |
| `populate-sanity-videos.py` | Batch-create Sanity drafts from existing B2 edited files | `python3 scripts/populate-sanity-videos.py --live` | No |
| `transcribe-with-speakers.py` | Standalone re-transcription (Whisper + pyannote) | `python3 scripts/transcribe-with-speakers.py "path.mp4"` | No |
| `audit-faststart.py` | Check MP4 files for faststart (MOOV atom position) | `python3 scripts/audit-faststart.py path.mp4` | No |
| `audit-sanity-integrity.py` | Verify Sanity docs match actual B2 files (CDN URL audit) | `python3 scripts/audit-sanity-integrity.py` | No |

**Compression Settings table** — Fix the outdated bitrate values (per D-04). Replace the existing table with:

| Parameter | Value |
|-----------|-------|
| Codec | H.264 (libx264) |
| Preset | slow |
| CRF | 18 |
| Pixel format | yuv420p |
| Optimization | faststart (MOOV atom at start for progressive streaming) |

Remove the "Bitrate: 18 Mbps (max 25 Mbps)" row entirely. CRF 18 is quality-based — no bitrate cap.

**Section ordering** for the rewritten doc:
1. Overview (with ecosystem context per D-08)
2. Pipeline Flow (expanded ASCII per D-03)
3. Quick Start (placeholder heading — Task 2 fills this)
4. Script Reference (updated table per D-05)
5. Camera Profiles and LUTs (keep existing — still accurate)
6. Compression Settings (updated — no bitrate cap)
7. B2 Bucket Structure (keep existing — still accurate)
8. Sanity Video Schema (keep existing — still accurate)
9. Troubleshooting (placeholder heading — Task 2 fills this)
10. Current State (keep existing section, update date to March 2026)
11. Pending Work (keep existing checklist — still relevant)
  </action>
  <verify>
    <automated>grep -c "CRF" docs/MEDIA-PIPELINE.md | grep -q "[1-9]" && grep -c "pipeline.py" docs/MEDIA-PIPELINE.md | grep -q "[1-9]" && ! grep -q "18 Mbps" docs/MEDIA-PIPELINE.md && ! grep -q "max 25 Mbps" docs/MEDIA-PIPELINE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/MEDIA-PIPELINE.md contains "pipeline.py" at least 3 times (it is the centerpiece per D-05)
    - docs/MEDIA-PIPELINE.md contains "CRF | 18" or "CRF | 18" in the compression table
    - docs/MEDIA-PIPELINE.md does NOT contain "18 Mbps" or "max 25 Mbps" (bitrate cap removed per D-04)
    - docs/MEDIA-PIPELINE.md contains "Step 1:" through "Step 5:" in the pipeline flow diagram
    - docs/MEDIA-PIPELINE.md contains "## Quick Start" heading (placeholder for Task 2)
    - docs/MEDIA-PIPELINE.md contains "## Troubleshooting" heading (placeholder for Task 2)
    - docs/MEDIA-PIPELINE.md contains "process_video()" and "process_transcript()" (importability documented)
    - docs/MEDIA-PIPELINE.md contains "faststart" in compression settings
    - docs/MEDIA-PIPELINE.md contains "extract-speaker-clips.py" and "extract-dialogue-clips.py" in script table
    - docs/MEDIA-PIPELINE.md contains "audit-faststart.py" and "audit-sanity-integrity.py" in script table
    - B2 Bucket Structure section preserved (contains "hector-ecosystem-archive-prod")
    - Camera Profiles section preserved (contains "sony-a6700-slog3.cube")
  </acceptance_criteria>
  <done>Architecture overview rewritten with ecosystem context (D-08), pipeline flow shows pipeline.py as orchestrator with 5 steps (D-03), script reference table updated with all 8 scripts including standalone vs pipeline usage (D-05), compression settings fixed to CRF-18-only (D-04), accurate sections preserved unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Quick Start usage guide, flags reference, and troubleshooting section</name>
  <files>docs/MEDIA-PIPELINE.md</files>
  <read_first>
    - docs/MEDIA-PIPELINE.md (re-read after Task 1 modifications)
    - scripts/pipeline.py (lines 280-341 for full argparse definition, lines 1-15 for docstring usage examples)
    - scripts/process-raw-video.py (lines 47-66 for argparse, lines 69-73 for HF_TOKEN error)
    - .planning/phases/16-pipeline-documentation/16-CONTEXT.md (D-06 quick-start format, D-07 troubleshooting scope)
  </read_first>
  <action>
Fill in the Quick Start and Troubleshooting placeholder sections created in Task 1.

**## Quick Start** (per D-06 — quick command first, then reference table):

Add the following content under the Quick Start heading:

### Common Case (Sony A6700, full pipeline with Sanity docs)

```bash
export HF_TOKEN="hf_..."           # Hugging Face (pyannote diarization)
export SANITY_TOKEN="sk..."        # Sanity write token

python3 scripts/pipeline.py "Futuro MMXXV/raw/card-1/Day 1/C3460.MP4" --live
```

This downloads the raw file from B2, applies the Sony A6700 S-Log3 LUT, encodes H.264 CRF 18, transcribes with Whisper, diarizes with pyannote, extracts per-speaker clips, uploads everything to B2, and creates draft Sanity documents.

### Processing a folder (all videos in a directory)

```bash
python3 scripts/pipeline.py "Futuro MMXXV/raw/card-1/Day 1" --live
```

### Flags Reference

| Flag | Default | Effect |
|------|---------|--------|
| `b2_path` (positional) | required | B2 path to raw video file or folder |
| `--camera PROFILE` | `sony-a6700-slog3` | Camera profile for LUT selection. Options: `sony-a6700-slog3`, `canon-r5-clog3`, `gopro-hero7-standard`, `gopro-hero7-protune`, `iphone-12promax`, `rec709` |
| `--anamorphic` | off | Apply 1.33x horizontal desqueeze (Sirui 50mm anamorphic lens) |
| `--skip-transcribe` | off | Encode only — skip Whisper transcription and speaker diarization |
| `--skip-upload` | off | Process locally only — do not upload to B2 |
| `--skip-sanity` | off | Upload to B2 but do not create Sanity documents |
| `--skip-clips` | off | Encode and transcribe but skip speaker clip extraction |
| `--dry-run` | off | Preview Sanity mutations without executing them |
| `--live` | off | Execute Sanity mutations (creates draft documents) |

Note: `--dry-run` and `--live` control Sanity document creation only. Without either flag, the pipeline runs through upload but skips Sanity. Documents are always created as drafts (`drafts.` prefix) — editorial review in Sanity Studio is required before publishing.

### Camera Profile Examples

```bash
# Canon R5 C-Log3
python3 scripts/pipeline.py "Kah Foundry XXVI/raw/135A3217.MP4" --camera canon-r5-clog3 --live

# GoPro Hero 7 standard (sRGB)
python3 scripts/pipeline.py "Futuro MMXIX/raw/confessional.MP4" --camera gopro-hero7-standard --live

# Screen recording (no LUT)
python3 scripts/pipeline.py "Kah Foundry XXVI/raw/screen-recording.mp4" --camera rec709 --live

# Anamorphic footage (Sony A6700 + Sirui 50mm)
python3 scripts/pipeline.py "path/to/anamorphic.MP4" --anamorphic --live
```

### Prerequisites

- Python 3.10+
- FFmpeg with libx264 support (`brew install ffmpeg`)
- Backblaze B2 CLI authenticated (`b2 authorize-account`)
- LUT files in `luts/` directory (FilmConvert Nitrate exports)
- Python packages: `whisper`, `pyannote.audio`, `torch`

**## Troubleshooting** (per D-07 — 3-5 most common issues):

Add the following content under the Troubleshooting heading:

### `ERROR: HF_TOKEN environment variable not set`

pyannote speaker diarization requires a Hugging Face token with access to the pyannote models.

```bash
export HF_TOKEN="hf_..."
```

Get a token at https://huggingface.co/settings/tokens. You must also accept the pyannote model license at https://huggingface.co/pyannote/speaker-diarization-3.1.

### `ERROR: SANITY_TOKEN not set`

Only needed when using `--dry-run` or `--live`. Create a token in the Sanity management dashboard:

```bash
export SANITY_TOKEN="sk..."
```

Project ID: `fo6n8ceo`, Dataset: `production`.

### FFmpeg not found or missing libx264

```bash
brew install ffmpeg
ffmpeg -encoders | grep libx264  # verify
```

### B2 authentication failure

The pipeline uses the `b2` CLI for downloads and uploads. Authenticate first:

```bash
b2 authorize-account <applicationKeyId> <applicationKey>
```

Key must have read+write access to `hector-ecosystem-archive-prod`.

### Mid-pipeline failure / resuming

The pipeline processes each video independently. If a batch run (folder mode) fails partway through, re-run the same command — already-uploaded B2 files will be overwritten (idempotent), and `check_existing_b2key()` prevents duplicate Sanity documents in `--live` mode.

For single-file failures, check `/tmp/b2-raw-process/` for intermediate files. The pipeline cleans up after success but leaves files on failure for debugging.
  </action>
  <verify>
    <automated>grep -c "## Quick Start" docs/MEDIA-PIPELINE.md | grep -q "1" && grep -c "## Troubleshooting" docs/MEDIA-PIPELINE.md | grep -q "1" && grep -c "\-\-live" docs/MEDIA-PIPELINE.md | grep -q "[2-9]" && grep -q "HF_TOKEN" docs/MEDIA-PIPELINE.md && grep -q "SANITY_TOKEN" docs/MEDIA-PIPELINE.md && grep -q "b2 authorize-account" docs/MEDIA-PIPELINE.md && grep -q "Flags Reference" docs/MEDIA-PIPELINE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/MEDIA-PIPELINE.md contains exactly one "## Quick Start" heading
    - docs/MEDIA-PIPELINE.md contains exactly one "## Troubleshooting" heading
    - docs/MEDIA-PIPELINE.md contains "### Flags Reference" with a table listing all 9 flags (b2_path, --camera, --anamorphic, --skip-transcribe, --skip-upload, --skip-sanity, --skip-clips, --dry-run, --live)
    - docs/MEDIA-PIPELINE.md contains "--live" at least 4 times (quick start examples + flags table)
    - docs/MEDIA-PIPELINE.md contains "HF_TOKEN" in the troubleshooting section
    - docs/MEDIA-PIPELINE.md contains "SANITY_TOKEN" in the troubleshooting section
    - docs/MEDIA-PIPELINE.md contains "b2 authorize-account" in the troubleshooting section
    - docs/MEDIA-PIPELINE.md contains "brew install ffmpeg" in the troubleshooting section
    - docs/MEDIA-PIPELINE.md contains "### Prerequisites" with Python 3.10+, FFmpeg, B2 CLI, LUT files
    - docs/MEDIA-PIPELINE.md contains "### Camera Profile Examples" with at least 3 different --camera values
    - docs/MEDIA-PIPELINE.md contains "Mid-pipeline failure" or "resuming" in troubleshooting
    - docs/MEDIA-PIPELINE.md contains "check_existing_b2key" (deduplication note)
    - docs/MEDIA-PIPELINE.md contains "drafts." or "draft" explaining that docs are created as drafts
  </acceptance_criteria>
  <done>Quick Start section has one-command common case, folder processing example, full flags reference table with all 9 flags, camera profile examples for 4 camera types, and prerequisites list. Troubleshooting section covers 5 failure modes: HF_TOKEN missing, SANITY_TOKEN missing, FFmpeg not found, B2 auth failure, and mid-pipeline resume strategy.</done>
</task>

</tasks>

<verification>
1. `grep -c "pipeline.py" docs/MEDIA-PIPELINE.md` returns 5+ (centerpiece of doc)
2. `! grep -q "18 Mbps" docs/MEDIA-PIPELINE.md` (no bitrate cap)
3. `grep -c "## " docs/MEDIA-PIPELINE.md` returns 8+ (all major sections present)
4. `grep -q "HF_TOKEN" docs/MEDIA-PIPELINE.md && grep -q "SANITY_TOKEN" docs/MEDIA-PIPELINE.md` (env vars documented)
5. `grep -q "python3 scripts/pipeline.py" docs/MEDIA-PIPELINE.md` (runnable command in doc)
</verification>

<success_criteria>
- A new collaborator reading only docs/MEDIA-PIPELINE.md can identify what each pipeline script does, its inputs/outputs, and where each component runs
- The Quick Start section provides a copy-pasteable command that processes a raw video end-to-end
- The Flags Reference table documents all 9 pipeline.py arguments
- No outdated bitrate references remain (CRF 18 only)
- Troubleshooting covers HF_TOKEN, SANITY_TOKEN, FFmpeg, B2 auth, and mid-run failures
- pipeline.py is clearly documented as the single entry point
</success_criteria>

<output>
After completion, create `.planning/phases/16-pipeline-documentation/16-01-SUMMARY.md`
</output>
