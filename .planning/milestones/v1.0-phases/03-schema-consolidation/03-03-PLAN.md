---
phase: 03-schema-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - schemaTypes/siteSettings_hector.ts
  - schemaTypes/siteSettings_benext.ts
  - schemaTypes/siteSettings_next.ts
  - schemaTypes/siteSettings_mitikah.ts
  - schemaTypes/siteSettings_medikah.ts
  - schemaTypes/siteSettings_arkah.ts
  - schemaTypes/index.ts
  - schemaTypes/alumniContinuum.ts
  - deskStructure.ts
autonomous: true
requirements:
  - SCHM-04
  - SCHM-05

must_haves:
  truths:
    - "Every entity (hector, benext, futuro, next, mitikah, medikah, arkah) has a site settings document type"
    - "All 7 site settings types appear in the System section of desk structure"
    - "alumniContinuum is visible in the Companion Platform desk section (not hidden)"
    - "alumniContinuum description no longer has 'pending review' language"
  artifacts:
    - path: "schemaTypes/siteSettings_hector.ts"
      provides: "Hector site settings schema"
      contains: "siteSettings_hector"
    - path: "schemaTypes/siteSettings_benext.ts"
      provides: "BeNeXT site settings schema"
      contains: "siteSettings_benext"
    - path: "schemaTypes/siteSettings_next.ts"
      provides: "NeXT site settings schema"
      contains: "siteSettings_next"
    - path: "schemaTypes/siteSettings_mitikah.ts"
      provides: "Mitikah site settings schema"
      contains: "siteSettings_mitikah"
    - path: "schemaTypes/siteSettings_medikah.ts"
      provides: "Medikah site settings schema"
      contains: "siteSettings_medikah"
    - path: "schemaTypes/siteSettings_arkah.ts"
      provides: "Arkah site settings schema"
      contains: "siteSettings_arkah"
    - path: "schemaTypes/index.ts"
      provides: "All 6 new site settings registered in schema array"
      contains: "siteSettings_hector"
    - path: "deskStructure.ts"
      provides: "Desk structure with all 7 site settings and visible alumniContinuum"
  key_links:
    - from: "schemaTypes/index.ts"
      to: "6 new siteSettings type files"
      via: "import + schemaTypes array"
      pattern: "import siteSettings_"
    - from: "deskStructure.ts"
      to: "siteSettings types + alumniContinuum"
      via: "S.listItem and S.document for singleton pattern"
      pattern: "siteSettings_"
---

<objective>
Create site settings for all 7 entities and commit the alumniContinuum type.

Purpose: Currently only Futuro has a site settings document. All 7 entities need configuration documents for their frontends. Additionally, alumniContinuum has been in limbo as an experimental type -- it has a clear purpose (alumni-only curated content) and needs to be committed with proper desk placement.

Output: 6 new siteSettings schema files, updated schema registry, updated desk structure with all settings and visible alumniContinuum.
</objective>

<execution_context>
@/Users/hectorhlopez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hectorhlopez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-schema-consolidation/03-CONTEXT.md

<interfaces>
<!-- siteSettings_futuro.ts pattern (the template for new types): -->
<!-- Fields: siteTitle (required string), metaDescription (text), defaultSocialImage (image with hotspot), globalCta (object: copy + url), socialLinks (array of objects: platform + url + handle), footerCopy (text) -->
<!-- Uses defineType + defineField from 'sanity' -->
<!-- Preview: select title from siteTitle, subtitle from globalCta.copy, media from defaultSocialImage -->
<!-- Singleton pattern in desk: S.document().schemaType('siteSettings_futuro').documentId('siteSettings_futuro') -->

<!-- deskStructure.ts key sets: -->
<!-- groupedDocTypes: Set of all explicitly placed types (must add new siteSettings + remove alumniContinuum from legacy) -->
<!-- GOVERNED_TYPES: Set of types that get Governance/References tabs (alumniContinuum should be added) -->
<!-- alumniContinuum is currently in groupedDocTypes under "Legacy (hidden)" comment -->

<!-- schemaTypes/index.ts: -->
<!-- alumniContinuum currently imported under "Legacy (registered but hidden from desk)" comment -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 entity site settings schemas</name>
  <files>schemaTypes/siteSettings_hector.ts, schemaTypes/siteSettings_benext.ts, schemaTypes/siteSettings_next.ts, schemaTypes/siteSettings_mitikah.ts, schemaTypes/siteSettings_medikah.ts, schemaTypes/siteSettings_arkah.ts</files>
  <action>
Create 6 new schema files following the exact pattern from siteSettings_futuro.ts. Each file has the same base fields: siteTitle, metaDescription, defaultSocialImage, globalCta (object with copy + url), socialLinks (array of objects with platform + url + handle), footerCopy.

For each entity, only change the name, title, description, and preview title fallback:

| Entity | name | title | description (Global configuration powering...) |
|--------|------|-------|------------|
| hector | siteSettings_hector | Hector Site Settings | hectorhlopez.com defaults, metadata, and shared UI copy |
| benext | siteSettings_benext | BeNeXT Site Settings | benextglobal.com defaults, metadata, and shared UI copy |
| next | siteSettings_next | NeXT Site Settings | next.benextglobal.com defaults, metadata, and shared UI copy |
| mitikah | siteSettings_mitikah | Mitikah Site Settings | mitikah.com defaults, metadata, and shared UI copy |
| medikah | siteSettings_medikah | Medikah Site Settings | medikah.com defaults, metadata, and shared UI copy |
| arkah | siteSettings_arkah | Arkah Site Settings | arkah.com defaults, metadata, and shared UI copy |

Each file uses `import { defineType, defineField } from 'sanity'` and `export default defineType({...})`. Copy the exact field definitions from siteSettings_futuro.ts -- same validation, same nested object structures, same preview config (adjusting the fallback title string to match the entity name).
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && ls schemaTypes/siteSettings_*.ts | wc -l && echo "Expected: 7 (including futuro)" && for f in schemaTypes/siteSettings_{hector,benext,next,mitikah,medikah,arkah}.ts; do echo "$f exists: $(test -f $f && echo yes || echo NO)"; done</automated>
  </verify>
  <done>6 new siteSettings schema files exist, each following the siteSettings_futuro pattern with entity-specific name, title, and description.</done>
</task>

<task type="auto">
  <name>Task 2: Register new schemas, update desk structure, commit alumniContinuum</name>
  <files>schemaTypes/index.ts, deskStructure.ts, schemaTypes/alumniContinuum.ts</files>
  <action>
**schemaTypes/index.ts changes:**

1. Add imports for the 6 new site settings types in the "Core" section (near siteSettings_futuro):
   ```
   import siteSettings_hector from './siteSettings_hector'
   import siteSettings_benext from './siteSettings_benext'
   import siteSettings_next from './siteSettings_next'
   import siteSettings_mitikah from './siteSettings_mitikah'
   import siteSettings_medikah from './siteSettings_medikah'
   import siteSettings_arkah from './siteSettings_arkah'
   ```

2. Add them to the schemaTypes array near siteSettings_futuro in the Core section:
   ```
   siteSettings_hector,
   siteSettings_benext,
   siteSettings_next,
   siteSettings_mitikah,
   siteSettings_medikah,
   siteSettings_arkah,
   ```

3. Move alumniContinuum from the "Legacy (registered but hidden from desk)" comment section to the "Companion Platform" section (both import and array entry). Update the import comment to reflect it is no longer legacy.

**schemaTypes/alumniContinuum.ts changes:**

Update the description to remove "pending review" language. New description:
`'Alumni-only curated content — resources, opportunities, and announcements for the alumni community. ' + 'Default narrativeOwner: "benext". Default platformTier: "institutional". Default archivalStatus: "alumni-only". ' + 'Audience scoping controls which alumni group sees this content.'`

(Remove the line: "Status: pending review. May be kept or removed depending on Companion Platform decisions.")

**deskStructure.ts changes:**

1. **Add alumniContinuum to GOVERNED_TYPES set** (it uses governanceFields):
   Add `'alumniContinuum'` to the GOVERNED_TYPES Set.

2. **Move alumniContinuum from hidden to visible in groupedDocTypes:**
   Remove from the "Legacy (hidden)" comment group. Add to the "Tier 3" section or wherever companion platform types are listed. The key thing: it stays in groupedDocTypes but is no longer labeled as hidden/legacy.

3. **Add alumniContinuum to the Programs & Projects tier** in the desk items, in the companion platform sub-section (after `participantConnection`):
   ```
   listWithPreview('alumniContinuum', 'Alumni Continuum'),
   ```

4. **Add all 7 site settings to the System tier** as a grouped sub-list. Replace the single Futuro Site Settings entry with a "Site Settings" group containing all 7 as singletons:
   ```
   S.listItem()
     .title('Site Settings')
     .child(
       S.list()
         .title('Site Settings')
         .items([
           S.listItem()
             .title('Hector Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_hector')
                 .documentId('siteSettings_hector'),
             ),
           S.listItem()
             .title('BeNeXT Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_benext')
                 .documentId('siteSettings_benext'),
             ),
           S.listItem()
             .title('Futuro Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_futuro')
                 .documentId('siteSettings_futuro'),
             ),
           S.listItem()
             .title('NeXT Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_next')
                 .documentId('siteSettings_next'),
             ),
           S.listItem()
             .title('Mitikah Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_mitikah')
                 .documentId('siteSettings_mitikah'),
             ),
           S.listItem()
             .title('Medikah Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_medikah')
                 .documentId('siteSettings_medikah'),
             ),
           S.listItem()
             .title('Arkah Site Settings')
             .child(
               S.document()
                 .schemaType('siteSettings_arkah')
                 .documentId('siteSettings_arkah'),
             ),
         ]),
     ),
   ```

5. **Add all 6 new siteSettings type names to groupedDocTypes** set:
   `'siteSettings_hector', 'siteSettings_benext', 'siteSettings_next', 'siteSettings_mitikah', 'siteSettings_medikah', 'siteSettings_arkah'`
  </action>
  <verify>
    <automated>cd /Users/hectorhlopez/projects/clean-studio && npx sanity schema extract --enforce-required-fields 2>&1 | head -10 && grep -c "siteSettings_" schemaTypes/index.ts && echo "Expected: 14 (7 imports + 7 array entries)" && grep "alumniContinuum" deskStructure.ts | head -5 && grep "pending review" schemaTypes/alumniContinuum.ts; echo "Expected: no 'pending review' output"</automated>
  </verify>
  <done>All 7 entities have site settings types registered. Desk structure shows grouped Site Settings in System tier. alumniContinuum is visible in Programs and Projects tier (companion platform section), listed in GOVERNED_TYPES, and description updated to remove pending language.</done>
</task>

</tasks>

<verification>
- `npx sanity schema extract --enforce-required-fields` completes without errors
- 7 siteSettings files exist (futuro + 6 new)
- All 7 registered in schemaTypes/index.ts
- Desk structure has "Site Settings" group in System tier with all 7 entities
- alumniContinuum appears in Programs & Projects companion platform section
- alumniContinuum is in GOVERNED_TYPES set
- alumniContinuum description has no "pending review" language
- No type appears in ungrouped fallback that shouldn't
</verification>

<success_criteria>
- Every entity has a site settings document type
- All settings accessible via desk structure as singletons
- alumniContinuum committed: visible, governed, properly described
- Schema validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-consolidation/03-03-SUMMARY.md`
</output>
